import React, { Component } from 'react';
import { connect } from 'react-redux';

function mapStateToProps(state) {
    return {

    };
}

class menu extends Component {
    render() {
        return (
            <div>
                
            </div>
        );
    }
}
var wrapper = ({param1, param2}) => {

};

export default connect(
    mapStateToProps,
)(menu);